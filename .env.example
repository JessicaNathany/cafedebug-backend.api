# Docker Compose Environment Variables - REFERENCE ONLY
# 
# IMPORTANT: This file is for reference and local development only.
# In production, the .env file is automatically generated by GitHub Actions
# from GitHub Secrets during deployment. You do NOT need to manually create
# .env on the EC2 server.
#
# For local development:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your local/test values
# 3. DO NOT commit the .env file with real secrets to version control
#
# For production:
# Configure these values as GitHub Secrets (Settings → Secrets and variables → Actions)

# ============================================
# Docker Image Configuration
# ============================================
IMAGE_TAG=main-abc1234  # Set by GitHub Actions during deployment
ASPNETCORE_ENVIRONMENT=Production

# ============================================
# Caddy Reverse Proxy Configuration
# ============================================
CADDY_DOMAIN=api.cafedebug.com.br

# ============================================
# Database Configuration
# ============================================
# Format: Server=<host>;Port=<port>;Database=<db>;User=<user>;Password=<password>
DB_CONNECTION_STRING=Server=db.example.com;Port=3306;Database=cafedebug;User=cafedebug_user;Password=your-secure-password

# ============================================
# JWT Configuration
# ============================================
JWT_ISSUER=https://api.cafedebug.com.br
JWT_AUDIENCE=https://cafedebug.com.br
JWT_SIGNING_KEY=your-very-long-secure-key-minimum-32-characters-here-change-this
JWT_VALID_MINUTES=15
JWT_REFRESH_TOKEN_VALID_MINUTES=10080

# ============================================
# AWS S3 Storage Configuration
# ============================================
AWS_S3_BUCKET=cafedebug-images
AWS_S3_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_S3_BASE_URL=https://s3.amazonaws.com/cafedebug-images

# ============================================
# Email/SMTP Configuration
# ============================================
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
SMTP_FROM_EMAIL=noreply@cafedebug.com.br

# ============================================
# Logging Configuration (optional)
# ============================================
LOG_LEVEL=Information
LOG_PATH=/var/log/cafedebug/api

# ============================================
# Docker Swarm Replication
# ============================================
# These are typically managed by docker-compose deploy config, not environment
# replicas=3  # Set in deploy.sh or docker service update command
